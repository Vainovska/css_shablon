{{#with data}}
    <div class="post">
        <strong class="post_title">{{title}}</strong>
        <div class="post_info">{{date}} by <span class="post_aftor">{{author}}</span></div>
        <p class="post_deskription">{{description}}</p>
        <a href="#">Continue reading</a>
    </div>
{{/with}}